export type BootstrapColor =
  | "primary"
  | "secondary"
  | "error"
  | "warning"
  | "info"
  | "success"

const bootstrapColors: BootstrapColor[] = [
  "primary",
  "secondary",
  "error",
  "warning",
  "info",
  "success",
]
export default bootstrapColors
